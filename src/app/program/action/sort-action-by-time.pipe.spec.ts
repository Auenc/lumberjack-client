import { SortActionByTimePipe } from './sort-action-by-time.pipe';

describe('SortActionByTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SortActionByTimePipe();
    expect(pipe).toBeTruthy();
  });
});
